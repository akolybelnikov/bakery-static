export class Product {
  constructor(item) {
    this.image = item.image
    this.productName = item.productName
    this.price = item.price
    this.filling = item.filling
    this.weight = item.weight
    this.count = item.count
    this.total = item.total
  }
}
